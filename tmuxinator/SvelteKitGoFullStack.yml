name: SKit-Go
root: .

windows:
  - editor:
      panes:
        - cd ./frontend && lvim
  - server:
      layout: even-horizontal
      panes:
        - cd ./frontend && npm i && npm run dev
        - cd ./backend && go run main.go # get the db 

  - tools:
      panes:
        - firefox "http://localhost:3000" && postman
        - podman-compose up -d
        - podman container list
  
  - monitoring:
      panes:
        - htop

