name: Svelte/Rust
root: .

windows:
  - editor:
      panes:
        - nvim
  - server:
      panes:
        - cd ../rust-backend && cargo run
        - cd ../ && npm run dev 
  - tools:
      panes:
        - firefox "http://localhost:3000" && firefox "http://localhost:8000"
  
  - monitoring:
      panes:
        - htop

